{
  "buildCommand": "corepack enable && pnpm install && pnpm run build",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "routes": [
    {
      "src": "/seed",
      "status": 404,
      "continue": true
    }
  ]
}
